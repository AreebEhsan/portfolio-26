"use client";

// 3D globe removed: keeping a minimal stub so imports won't break if used anywhere.
// This renders nothing but keeps the API surface if needed later.

export function HeroScene() {
  return null;
}
